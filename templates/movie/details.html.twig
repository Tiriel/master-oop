{% extends 'base.html.twig' %}

{% block title %}Hello MovieController!{% endblock %}

{% block body %}

    <div class="example-wrapper">
        <h1>Movie : {{ movie.title }}</h1>

        <ul>
            <li>Release date: {{ movie.released|date('Y M d') }}</li>
            <li>Genres: {{ movie.genre|join(', ') }}</li>
        </ul>
    </div>
{% endblock %}
